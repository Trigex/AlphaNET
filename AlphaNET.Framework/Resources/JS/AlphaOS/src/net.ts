/// <reference path="kernel/types/os.d.ts" />
/// <reference path="kernel/system.ts" />
function net(args: string[]) {
    switch(args[0]) {
        case "test":

            break;
    }
}

net(args);