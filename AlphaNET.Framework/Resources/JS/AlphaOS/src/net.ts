/// <reference path="kernel/types/os.d.ts" />
function Main() {
    var socket;
    if(Global.ProcessArguments[1]) {
        switch(Global.ProcessArguments[1]) {
            case "client":
                break;
            case "server":
                break;
        }
    } else {

    }
}